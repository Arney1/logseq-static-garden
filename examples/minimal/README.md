# Minimal garden

A hand-written Transit fixture with fictional notes, code, an equation, links, and a page embed. No personal graph data or Logseq runtime is included. The inline data assignment matches the format the exporter reads; this file is a build input, not the website to deploy.
